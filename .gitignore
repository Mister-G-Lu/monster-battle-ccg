# Build artifacts
build/decoded_apk/
# Removed web shell (was the HTML campaign prototype — scrapped)
build/web/
build/English_offline_unsigned.apk
build/English_offline_signed.apk
build/English_offline_aligned.apk
build/offline.keystore
build/assets/
build/orig_lua/

# Temp directories
sim_save/
sim_save_int/
java_out/
java_out2/
jadx_tool/
apk_contents/
lua/
lua51/
build/decoded_apk/

# Python
__pycache__/
*.pyc

# OS
.DS_Store
Thumbs.db

# Original APK (too large for git, user provides)
English.apk

# Encrypted Lua files (we track decrypted versions)
*.mu

# APK workspace temp files
patch_server.py
patch_battle.py
zipalign.py
dbg_battle.lua
sim_test.py

# Headless test fixtures (regenerated by scripts/setup_test_env.py):
# web-poc/ = feasibility spike for a browser build (Lua engine in WASM via wasmoon)
node_modules/
web-poc/node_modules/
web-poc/package-lock.json

# web/ = the real browser build (Vite + wasmoon). Generated assets are copied
# from decrypted/ + csv_plain/ by web/scripts/prepare_web.py at dev/build time.
web/node_modules/
web/dist/
web/public/game/
web/public/csv/
web/public/web_bridge.lua
web/public/game-manifest.json
# the source bridge lives in web/lua/ — un-ignore it (the broad `lua/` rule
# above, meant for decompiled output dirs, would otherwise swallow it)
!web/lua/
!web/lua/web_bridge.lua

# decrypted/ = game Lua tree decrypted from the APK's src.mu with src/ overlaid
# csv_plain/ = plain-text copies of csv_data/*.csv consumed by the tests' aandm stub
decrypted/
csv_plain/
sim_save/
sim_save_int/
sim_save_guide/
sim_save_web/
